# Parking 프로젝트 코딩 규칙
"당신은 언리얼, c++, 파이썬, 블루프린트 전문가 입니다."

**상세 문서화**: 변경 사항, 신규 클래스, 수정 로직에 대해 한글 `.md` 파일로 상세히 기록한다. ( yyyyMMdd_HHmmss_이름.md 형식의 파일이름, Docs 폴더에 저장, 질문에 대한 대답도 md파일 저장, 저장포맷:utf-8형식 )
   - **예외 — MCP 런타임 조작**: Park3D MCP/RPC로 실행 중인 인스턴스를 조작·조회만 하고 저장소 파일(`Park3D/Source`·`Config`·`Content`·스크립트·문서 등)을 **하나도 변경하지 않은** 작업은 `Docs/*.md`를 생성하지 않는다. 결과·수치·실패·미검증은 대화 응답으로 사실대로 보고한다. 파일을 하나라도 변경했거나 사용자가 문서화를 요청하면 예외를 적용하지 않는다.


## Unreal/Park3D 규약

- 단위: 미터→센티미터(×100).
- 축: Unity `(x, y_up, z)` → UE `(x, z, y_up=Z)`; 주차면은 UE XY 평면, 높이는 Z축.
- 개별 면 회전(`faceRot`)과 그룹 회전(`groupRot`)을 분리한다.
- 사선 보정은 `Default + |cos| > 0`일 때 폭/`cos(faceRot)` 간격을 사용하고, 방향 반전은 정규화 각도 > 180° 규약을 따른다.
- WBP 변경은 컴파일·저장 후 PIE를 재실행해 확인한다. BindWidget 이름·타입과 기존 Blueprint/에셋 참조를 보존한다.

## 변경 이력
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-08-27 | `car.deleteFile` 신설 — `car.save` 가 만든 배치 파일을 원격에서 지운다(`car.save`·`car.load` 와 **같은 파라미터 해석**, 없는 파일은 `existed:false` 로 성공) | Rpc/Modules/CarFilePaths.h(신규), CarRpcModule.*, Tests/CarFilePathsTest.cpp(신규), Docs/20260827_153938_* | SettingManager/TourAgent 요청(마스터 지시). 레시피를 저장할 때마다 `touragent-scene-*.json` 이 이 기계에 쌓이는데 **만들 수만 있고 지울 수 없어**(`file.list`·`file.delete` 는 `-32601`, `car.delete*` 는 월드의 차량을 지우는 것) 화면이 *"그 기계에서 사람이 지워야 합니다"* 라고 말하고 있었다. ⚠ **삭제는 저장과 대칭이 아니다** — 이 서버는 `AllowAnonymous` 라 `fullPath` 를 그대로 믿으면 임의 파일 삭제 통로가 된다 → 허용 폴더(`Saved/CarData`·`Save/3D/CarPos`)와 `.json` 으로 가둔다. 검문의 핵심은 **비교 전에 `..` 을 접는 것**(안 접으면 `…/CarData/../../Config/x.json` 이 접두사 일치로 통과한다)과 `Root + "/"` 로 비교하는 것(`CarData` ≠ `CarDataBackup`). 미검증: 작성이 리눅스 세션이라 **컴파일·자동화 테스트·실기 왕복을 못 했다**(윈도우 빌드 필요) |
| 2026-08-26 | 스트림 슬롯·예산을 `config_pmaker.json` 으로 이관(`stream_slots`·`stream_hard_max_slots`·`stream_total_fps`) — config > ini | Config/Park3DAppConfig.*, Rpc/CamStreamSubsystem.cpp, Config/DefaultGame.ini, Save/Config/config_pmaker.json, Tests/Park3DAppConfigTest.cpp | 사용자 지적. 바로 앞 항목에서 새 knob 을 ini 에만 두어 "pak 에 구워져 재쿡 없이는 못 바꾼다"는 함정을 스스로 만들었는데, **이 프로젝트는 같은 문제를 이미 같은 방법으로 풀어 뒀다** — `Park3DAppConfig.h:110` 의 "ini 는 패키지에 쿠킹돼 인스턴스별로 바꿀 수 없으므로 config 로 덮을 길을 연다(rpc_port 와 같은 규약)". 새 항목에만 그 규약을 안 지킨 게 원인. 적용 순서는 **상한 → 슬롯 → 예산**(상한을 먼저 안 올리면 슬롯이 옛 상한에 잘린다). **0·음수는 미지정으로 처리**한다 — 그대로 쓰면 슬롯 0(아무도 못 봄)·fps 0(0 나눗셈)이라 오타 한 글자로 스트림이 죽는다. 실증: config 6/3/8 vs ini 10/10/5 → 6/3/8 적용, 이어서 **JSON 만 고쳐 재기동**(exe LastWriteTime 동일)하니 10/10/5 로 바뀜. 대가: 값이 두 곳에 생겨 **config 에 키가 있으면 ini 를 고쳐도 조용히 무시된다** → 판정은 기동 로그의 `(출처: ...)` 한 줄로만 한다 |
| 2026-08-26 | 번호판 번호를 **양각(압출 지오메트리)** 으로 전환 — Text3D 플러그인 도입, 로딩 시 앞/뒤 판에 1회 생성. 평면 위젯은 폴백으로 유지 | Park3D.uproject, Park3D.Build.cs, Config/DefaultText3D.ini(신규), Source/Park3D/CarActor.* | 실물 번호판은 고시상 문자가 1.3~1.6mm 양각인데 우리 번호는 Unlit 위젯이라 조명을 아예 못 받는 스티커였다. **함정 다섯 — ① 셰이더를 등록하는 플러그인(Text3D→GeometryMask)을 추가하면 `Missing global shader` 로 즉사해 exe 교체 검증이 불가능하다(전체 재쿡 필요). 검증은 `UnrealEditor -game` 으로 한다. 패키지 루트 exe 는 171KB 부트스트랩이고 진짜는 `Park3D/Binaries/Win64` 다 ② Text3D 의 자동 영역 맞춤(Max*)은 `Scale.X = Scale.Y` 라 **압출 두께까지** 깎고(1.5mm→0.06mm), Max 값이 월드 cm 로 비교되지도 않아 폰트를 키우자 글자가 2.2cm 로 줄고 메모리가 21GB 로 치솟았다 → 끄고 실측 환산으로 폰트 크기를 직접 준다 ③ 머티리얼을 생성자에서 걸면 빌드가 덮어 글자가 희게 나오고, 런타임에 4종을 다 지정해도 **측벽만 흰색으로 남는다** → 빌드가 만든 글자 메시 슬롯을 순회해 직접 칠한다 ④ 압출 두께는 0.1cm 로 양자화된다 — 두 점만 보고 직선을 그었다가 세 번째 점에 반증당했다(실효 1.0mm) ⑤ 레거시 TextRender 가 실제로 그려지고 있어 양각 뒤에 흰 글자가 깔렸다. 지역변수 `Children` 은 `AActor::Children` 을 가려 C4458 이 에러가 된다** |
| 2026-08-26 | 카메라 스트림 슬롯 상한 2→10, fps 예산을 **슬롯 수가 아니라 실제 서빙 중인 채널 수**로 배분(`cam.streamStatus` 에 `activeChannels` 신설), 틱당 캡처 상한 추가 | Rpc/CamStreamSubsystem.*, CamStreamPolicy.*, Config/DefaultGame.ini, Tests/CamStreamPolicyTest.cpp | 팀 보드 요청 #313. **상한만 올리면 아무 소용이 없다** — `channelFps = TotalFps/ActiveSlots` 라 슬롯 10 을 열면 혼자 보는 사람이 5/10=0.5fps 가 되어 아무도 상한을 못 올린다. 나눗수를 실사용 채널로 바꾸니 총 캡처량이 항상 TotalFps 로 묶여 **슬롯을 열어 두는 것 자체가 공짜**가 됐다(실측 1명 5.69fps, 2명 2.68/2.69, 합계≈5). 요청의 Ask 3("슬롯 초과 연결을 거절하라")은 **전제가 재현되지 않아 하지 않았다** — 변경 전 빌드에서 슬롯 없는 채널도 순환으로 프레임을 받는다(25초에 6.8MB·바운더리 50개). 함정 둘: ① 순회 커서를 루프 안에서 갱신하면 인덱스가 밀려 채널이 그 틱에 통째로 빠진다(목표 2.5 → 실측 1.84) ② 틱당 1장 상한은 처리량 천장을 곧 틱 fps 로 만든다(틱 2.5 → sum(fps) 2.5) → 실효 상한에 `ceil(TotalFps/틱fps)` 바닥을 뒀다. **패키지의 `DefaultGame.ini` 는 pak 안에 구워져 exe 교체로 반영되지 않는다** — 재쿡 전에는 `Park3D/Saved/Config/Windows/Game.ini` 사용자 레이어로 덮어야 한다 |
| 2026-08-21 | 번호판 표기 `672 우 3269` → `672우 3269` + 번호 영역을 파란 KOR 스트립 오른쪽~판 오른쪽 끝으로 옮겨 중앙 정렬 | Source/Park3D/CarActor.*, CarPlateNumberWidget.* | 번호가 스트립에 닿아 인식이 나빴다. 원인은 위젯이 **판 중심**에 놓여 번호 중심 = 판 중심이었던 것. 영역을 옮기는 데 3D 컴포넌트 오프셋(`MeshWide` 방향) 대신 **위젯을 판에 1:1 로 덮고 안쪽 여백**으로 잡았다 — 판 로컬 축이 화면 좌/우 어느 쪽인지는 앞판·뒷판 회전을 따라가야 알 수 있고, 같은 취지의 `PlateTextSideShift` 주석은 TextRender 가 런타임 폰트라 화면에 안 그려져 **한 번도 검증된 적이 없다**. Slate 안쪽은 "글자가 뒤집혀 보이지 않는다"만으로 좌/우가 확정된다. 실측: 파란 폭은 큐브(2~6cm)가 아니라 **0~5.4cm**(메시 재질의 KOR 필드와 겹친다) — 여백 재조정은 캡처의 파란 폭을 기준으로 할 것 |
| 2026-06-24 | WBP(UMG) 디자이너 MCP 조정법 스킬 추가(위젯 배치·크기·위치·패딩·폰트·색상·콤보 스타일·정렬) | skills/unreal-umg-designer | 차량배치 UI 작업에서 익힌 방법 재사용 |
| 2026-07-28 | MCP 런타임 조작 시 문서 생략 예외 추가(3번 규칙) | CLAUDE.md, AGENTS.md | 소스 변경 없이 Park3D MCP/RPC로 조작·조회만 한 작업까지 Docs 문서를 남겨 잡음이 쌓임 → 저장소 파일 변경 여부를 기준으로 문서화 게이트 |
| 2026-07-29 | 반복 승인 자동화 스킬 + settings.json 허용 규칙 일반화 | .claude/skills/park3d-auto-approve, .claude/settings.json | 빌드·테스트·기동·RPC 승인이 인자만 달라도 매번 재요청됨(항목이 일회성 명령 통째로 박혀 있었음) → 명령 단위 패턴으로 일반화하고, 되돌리기 어려운 조작·코드 진행 방향 선택은 ask로 못 박음 |
| 2026-08-04 | 승인 규칙에 조회형/변경형 비대칭 경계 도입 + 포트 무관 네트워크 패턴 + MCP 브리지 실행 범주 추가 | .claude/settings.json, .claude/skills/park3d-auto-approve | 포트가 박힌 승인 항목이 포트 변경(13120→13510)으로 한꺼번에 무효화된 사고 → 네트워크 허용은 포트를 박지 않고 `http://localhost:*` 로 끊고, 호스트 방화벽·서비스·레지스트리 변경처럼 호스트 상태를 바꾸는 조작은 조회형과 분리해 ask로 못 박음 |
| 2026-08-05 | 조명 설정 패널 추가(노출·태양 광량/색/고도/방위·하늘빛 6항목, 저장/열기/적용, 시작 시 파일 적용). 패널 UI는 예외적으로 C++ 위젯 트리로 구성 | Source/Park3D/Light/*, MainMenuWidget, Park3DGameMode, Save/3D/Light/ | 한낮 조명 상향 후 `PP_FixedExposure` 고정값 0(달빛 기준)이 드러나 화면이 하얗게 탐. 에디터·Unreal MCP 미기동 + UE5.8 파이썬에 `WidgetBlueprint.WidgetTree` 미노출로 WBP 제작 불가 → C++ 구성으로 우회 |
| 2026-08-06 | 전면 허용을 user 스코프(`~/.claude/settings.json`)로 확대 + ask 가드레일 42건 동반 이관, 스킬에 "어느 파일에 넣어야 세션 시작부터 먹는가" 절 추가 | ~/.claude/settings.json, .claude/skills/park3d-auto-approve | 설정은 세션 시작 시 읽히므로 세션 중 편집은 그 세션에 안 먹는다. project 스코프만으로는 다른 폴더에서 시작한 세션에 적용되지 않아 "세션 시작할 때부터" 요구를 못 채움. 단 user 에 전면 허용만 넣고 ask 를 빠뜨리면 다른 프로젝트가 무방비가 되므로 가드레일을 함께 이관 |
| 2026-08-06 | 전면 허용 규칙 문법 오류 수정(`Bash(*)`→`Bash`, `PowerShell(*)`→`PowerShell`) + 스킬에 문법 함정 명시 | .claude/settings.json, .claude/skills/park3d-auto-approve | `Bash(*)` 는 "`*` 로 시작하는 명령" 패턴으로 해석되어 아무것도 매치하지 않는데 전면 허용으로 오인 → 전면 허용을 적용했는데도 승인 팝업이 계속 떴음. 도구 전체 허용은 괄호 없이 도구 이름만 적는다 |
| 2026-08-07 | 개발 하네스 제거 — 5-에이전트 팀(architect·impact-analyst·unreal-implementer·qa-verifier·doc-writer)과 대응 스킬 7종, Codex 어댑터(AGENTS.md 하네스 절·.codex/agents) 삭제. 규칙 0~5와 표준 실행 순서는 단독 작업 기준으로 유지 | .claude/agents, .claude/skills, .agents/skills, .codex, CLAUDE.md, AGENTS.md | 사용자 요청. 삭제된 하네스 파일의 상세 이력은 git 이력에서 조회 |
| 2026-08-11 | RPC 미구현 10건 중 9건 결선(cam.save/load/applyPreset, car.setMetallic, preset.setBoxVisible, random.slotPlace/slotJitter/frontBack/randomizeAll). placeInView만 보류 | Source/Park3D/CarColorComponent.*, ParkingPresetManager.*, Rpc/Modules/{Cam,Car,Preset,Random}RpcModule.* | 미구현 사유 5건이 낡았거나 틀렸음 — 슬롯 백엔드(`ComputeSlotCorners`)는 데칼 작업 때 이미 생겼고, setBoxVisible은 면 단위가 아니라 프리셋 단위였다. 사유 메시지를 사실로 유지하지 않으면 이미 가능한 기능이 계속 막혀 보인다 |
| 2026-08-11 | 카메라 뷰어 표시/숨김 단축키(Ctrl+Space) 추가 | Park3DGameMode.* | 뷰어에 메뉴와 같은 RemoveFromParent/AddToViewport 를 쓰면 로그상 재추가는 되는데 화면에 안 나온다(최초 1회 화면비로 크기 확정) → 뷰포트에서 빼지 않고 가시성만 토글하고, 히트테스트가 죽지 않도록 숨기기 직전 가시성을 저장해 복원한다 |
| 2026-08-12 | 출차 시뮬레이션 추가(주차면→출구 주행 후 차량 제거). 기본 후면주차·전면/랜덤 선택, F8 단축키 + HUD 출차 버튼 + `sim.exit` RPC | Source/Park3D/Sim/*, Rpc/Modules/SimRpcModule.cpp, Park3DGameMode.* | 입차와 경로 골격이 같아 방향(`EParkSimDir`)만 뒤집어 재사용했다. 후진 구간이 방향별로 달라(입차 후면=마지막 구간, 출차 전면=첫 구간) `bBackIn` 불리언을 구간 인덱스로 일반화. 멤버명을 `Dir` 로 두면 로그 함수의 지역변수와 겹쳐 C4458 이 에러로 승격되므로 `RunDir` |
| 2026-08-11 | 주차 진입 시뮬레이션 추가(입구→통로→진입점→면 중심 주행, 이동/정지/주차 로그, 리플레이). F9/F10 단축키 + 좌하단 HUD + sim.* RPC 5종 | Source/Park3D/Sim/*, Rpc/Modules/SimRpcModule.*, RpcServerSubsystem.*, Park3DGameMode.* | 프리셋 목록 이원화 때문에 시작 시 자동 로딩분이 `AParkingPresetManager::StoredPresets`에 없다 → 시뮬은 매니저 목록이 비면 `config_pmaker.json`의 preset_file을 직접 읽는다. 주차면 사각형은 `ComputeSlotCorners`를 공유해 렌더/차량배치와 같은 기하 위에서 움직인다 |
| 2026-08-12 | 회피 대상을 주차장의 모든 차량으로 확대(서 있는 차에 막히면 `정체중단`), 입차는 빈 면만·출차는 서 있는 차를 인수(없을 때만 생성) | Source/Park3D/Sim/ParkingSimManager.* | "그냥 통과한다" 신고의 원인이 셋이었다 — ① 신고 화면(13510)이 회피 없는 옛 빌드였고 ② 회피가 시뮬 주행 차량만 봐서 주차된 65대를 무시했고 ③ **도착점 자체가 겹쳤다**(입차가 찬 면을 목표로, 출차가 찬 면에 새 차를 스폰). 주행 중 회피로는 ③을 못 막는다. 서 있는 차는 비켜 주지 않으므로 뚫지 않고 접는다. 마지막 구간은 목적지까지만 감시해야 뒷열 주차 차량을 오인하지 않는다. `CollectOccupiedSlots` 가 끝난 주행의 면까지 잡으면 출차가 그 차를 못 빼낸다 |
| 2026-08-12 | 시뮬 차량 간 충돌 회피(종방향 감속·정지, 4초 교착 시 RunId 우선순위 통과, 출차 통로 2.5m 바깥 오프셋) + MCP 브리지 호출별 `port` 인자 | Source/Park3D/Sim/ParkingSimManager.*, park3d-rpc-mcp/src/server.ts | 조향 회피는 통로가 좁아 주차면으로 파고들 위험이 커서 속도만 조절한다. 마주 선 두 대가 굳지 않도록 "작은 RunId 만 통과"라는 순서 규칙으로 사이클을 없앴다(대신 그 구간은 겹친다). 개발 exe 는 Zen 스토어를 찾다 죽어 단독 실행이 안 되므로 패키지를 복제해 exe 만 갈아끼운다. 브리지는 **재연결 전까지 `port` 를 조용히 무시**하므로 `system.health` 의 port 로 대상을 확인할 것 |
| 2026-08-12 | 마우스 휠 줌 + Left Alt+방향키 화면기준 이동 | Source/Park3D/ParkFlyPawn.*, CarPlacementWidget.cpp, PresetMakerWidget.cpp | 휠은 축이 아니라 칸마다 Pressed 로 들어와 BindKey 로 받고, 가감속이 끼면 칸당 이동량이 흔들려 MovementComponent 대신 액터를 직접 옮긴다. 방향키는 이미 세 곳(DefaultPawn 전후진 축·차량 배치 이동·프리셋 오프셋 제어)이 써서 Alt 보유 중 전부 양보시키지 않으면 한 번 눌러 둘이 움직인다. Alt 는 PIE 가 가로채는 전례가 있어 PlayerController 폴링 + Slate 수정자 이중으로 본다 |
| 2026-08-13 | `view.set` 화각 잠금/해제 로깅 추가 | Rpc/Modules/ViewRpcModule.cpp | "화면 비율이 이상하다" 신고의 원인이 `LockedFOV=138`(런타임 잠금)이었는데 흔적이 없어 누가 언제 걸었는지 특정할 수 없었다. `DefaultFOV` 는 멀쩡했고(`GetFOVAngle` 이 LockedFOV 를 우선) 코드·블루프린트에 `SetFOV` 호출부는 `view.set` 하나뿐이라 외부 호출로 판정. 앞뒤 값을 남겨 다음엔 로그 한 줄로 끝나게 했다. `ApplyMainViewFov` 의 16:9 하드코딩(창 비율이 다르면 의도한 수직 45° 가 어긋남)은 이번 원인이 아니라 보류 |
| 2026-08-12 | 차량 배치 패널 "차량 숨기기" 체크박스 + `car.hideAll` RPC(전체 차량 표시/숨김) | CarPlacementManager.*, CarPlacementWidget.*, Rpc/Modules/CarRpcModule.* | 동작은 매니저(`SetAllCarsHidden`)가 소유하고 체크박스·RPC 가 그것만 부른다(리셋랜덤 선례). 체크 상태를 위젯에 들면 RPC 로 숨긴 뒤 패널을 열 때 어긋나므로 열릴 때마다 월드에서 읽는다. **쿠킹된 WBP 의 C++ 베이스에 `UPROPERTY` 를 추가하면 패키지가 `Bad export index` 로 즉사한다 — 함수만 추가할 때와 달리 exe 교체로 안 되고 콘텐츠 재쿠킹이 필요**. 인자 `bHidden` 은 `AActor` 멤버와 겹쳐 UHT 가 막으므로 `bInHidden` |
| 2026-08-12 | 주차 시뮬레이션 HUD ↔ 메뉴 패널 배타 표시(둘 중 하나만 뜬다) | MainMenuWidget.*, Park3DGameMode.* | 메뉴 패널끼리는 이미 배타적이었지만 HUD 의 주인은 게임모드라(F8/F9/F10 단축키가 같은 인스턴스를 쓴다) 두 그룹이 서로를 몰랐다. 남의 위젯을 직접 빼면 ZOrder·가시성 규칙이 두 곳으로 갈라지므로, 소유자마다 "너희 것만 접어라" 함수(`HideAllPanels`/`HideSimPanel`)를 두고 여는 쪽이 상대에게 요청한다. 숨기기 전용이라 상호 호출이 사이클을 만들지 않는다. 메인 메뉴는 HUD 를 여는 버튼이 거기 있어 제외 |
| 2026-08-12 | 시뮬레이션 동시 주행(액터 1개 = 주행 1건, RunId) + RPC 대칭화(`sim.start`/`sim.scenario` 에 `dir`, `runId` 라우팅, `sim.list` 신설) | Source/Park3D/Sim/*, Rpc/Modules/SimRpcModule.*, Park3DGameMode.* | 주행 상태가 전부 액터 멤버라 로직을 안 고치고 인스턴스를 늘리는 쪽이 안전했다. 동시 실행이 되자 숨어 있던 충돌이 드러남 — 같은 면 중복 선정, 차량 id(`{idx}-{HH.mm.ss}`) 같은 초 충돌, 로그 파일명이 타임스탬프뿐이라 같은 초 완료분이 덮임. 셋 다 막았고 차량 간 충돌 회피는 범위 밖 |
| 2026-08-13 | JSON 저장 4곳에 `ForceUTF8WithoutBOM` 명시(카메라 위치·뷰어 크기·차량 배치·주차 프리셋) | CameraControlLibrary.cpp, CameraViewerWidget.cpp, CarPlacementLibrary.cpp, PresetMakerWidget.cpp | `SaveStringToFile` 는 인코딩을 생략하면 `AutoDetect` 이고, `FCString::IsPureAnsi` 가 거짓이면(문자 하나라도 `>0x7f`) 파일 **전체**를 UTF-16LE+BOM 으로 쓴다(엔진 `FileHelper.cpp:787`). `prefabName` 이 `기아_카니발` 같은 한글이라 차량 배치 저장은 사실상 항상 UTF-16 이었다 — 실증으로 첫 4바이트 `FF FE 7B 00` 확인. 이미 UTF-8 을 명시한 4곳(AppConfig·MapFloor·LightControl·SimManager)에 맞춰 BOM 없는 쪽으로 통일. 기존 UTF-16 파일은 `LoadFileToString` 이 BOM 으로 판별하므로 계속 읽힌다 |
| 2026-08-13 | `preset.create` 의 idx 중복·이름 덮어쓰기 수정(프리셋을 만들 때마다 idx=1, 이름은 0번에 박힘) | Rpc/Modules/PresetRpcModule.cpp | `FParkingPreset::PresetIdx` 기본값이 **1** 이라 `AddPreset` 의 자동 번호 조건(`<=0`)이 안 돌았다. idx 가 전부 1이 되자 `FindPresetByIdx`(첫 일치 반환)가 생성 직후 되찾기에서 0번을 집어 **매번 0번 이름을 덮어썼다**. 그 결과 `presetId` 로 지정 가능한 프리셋이 0번 하나뿐이었다(sim.start 포함). 매니저는 그대로 두고 전제를 깬 `preset.create` 만 고침 — idx 를 0으로 낮춰 번호를 위임하고, 되찾기는 `StoredPresets.Last()`. 깨진 런타임 목록은 `preset.load` 로 복구하되 **패키지에서는 `fileName` 이 아니라 `fullPath` 를 줘야 한다**(`Save/` 가 ProjectDir 밖) |
| 2026-08-13 | `car.assignPreset` 신설 — 차량 `presetId` 를 카메라 PTZ 프리셋 번호(1~5)로 재배정 | Rpc/Modules/CarRpcModule.cpp | 프리셋이 두 종류다 — 주차면(6개, idx 1~5·7)과 카메라 PTZ(5개, preset_id 1~5)이고 **1:1 이 아니다**(카메라 5개가 주차면 3열을 근/원으로 나눠 본다). 판정은 지면 조준점(`높이/tan(tilt)` 를 pan 방향으로) 최근접 — 화각 포함 판정은 프리셋 줌이 커서(zoom 6 → hfov 약 10°, 31m 에서 폭 5.5m) 대부분이 미배정이 된다. 카메라 프리셋은 **파일에서 직접 읽는다**(CamRpcModule 의 인메모리 목록은 `cam.loadPreset` 전까지 비어 있다). `slotId` 는 손대지 않았다 — `presetId`+`slotId` 쌍의 의미가 깨지지만 둘 다 바꾸면 더 모호해진다. `sim.start` 등의 `presetId` 는 여전히 주차면 idx 라 서로 다른 공간이다 |
| 2026-08-13 | 시나리오 1단계 — `light.*` RPC 5종 신설 + `cam.aimSlot` | Rpc/Modules/LightRpcModule.*(신규), CamRpcModule.cpp, RpcServerSubsystem.* | 조명이 RPC 표면에 아예 없었다(모듈 8개에 Light 없음) — 가림률을 픽셀로 재는데 측정 중 노출이 바뀌면 수치가 오염되므로 시나리오 정의에 조명이 들어가야 한다. 백엔드(`ALightControlManager::ApplySettings/CaptureCurrent`)는 이미 있어 결선만 했고, `light.set` 의 기준은 마지막 적용값이 아니라 **현재 월드 상태**다(패널로 바꾼 값을 조용히 되돌리지 않기 위해). `cam.aimSlot` 은 `pan 58.1/tilt 21.7/zoom 4.9` 같은 레이아웃 전용 숫자를 시나리오에서 없애기 위한 것 — 화각은 **슬랜트 거리**로 잡아야 하고(수평거리로 잡으면 내려다볼수록 좁아진다) `MaxZoom/DefaultHFov` 는 액터 인스턴스 값을 써야 한다(라이브러리 기본 인자는 config 오버라이드를 모른다) |
| 2026-08-13 | 작업 환경 분리 — 별도 포트 작업본(`Package/Work13520`, RPC 13520·cam 13701~13750) | (빌드 산출물, 커밋 제외) | 사용자 지시: 본래 실행창을 건드리지 말 것. exe 교체는 프로세스가 파일을 잡고 있으면 실패하므로 패키지를 `robocopy /MIR` 로 복제하고 exe 만 갈아끼운다. `-RpcPort=` 커맨드라인이 config 보다 우선. **카메라 스트림 포트 대역도 같이 옮겨야 한다** — 겹치면 두 인스턴스가 충돌한다. MCP 브리지는 호출별 `port` 인자를 실제로 따른다(`system.health` 로 확인). 부수 효과: 원본 인스턴스에서 `cam.setZoom` 이 먹지 않는 현상을 봤는데 작업본에서는 재현되지 않아 코드 버그가 아닌 그 인스턴스 쪽 간섭(패널 슬라이더 추정, 미확인)으로 판정 |
| 2026-08-13 | 시나리오 2단계 — `scenario.list/get/load/save` + 정적 가림 시나리오 3종 파일 | Rpc/Modules/ScenarioRpcModule.*(신규), RpcModuleSupport.*, CamRpcModule.cpp, RpcServerSubsystem.*, Save/3D/Scenario/*.json | 복원 순서가 `주차면→차량→조명→actors→카메라` 여야 한다 — actors 는 주차면 기하가 있어야 슬롯 좌표를 알고, 카메라는 `pos` 를 옮긴 **뒤** 각을 풀어야 한다. 대상을 `carNameId` 로 적지 않는다(id 가 `{idx}-{HH.mm.ss}` 시각 기반이라 재현마다 달라짐) — `role`+`(presetIdx,slot)` 로 적고 실제 id 는 응답으로 돌려준다. 겨냥 계산은 `RpcAim` 으로 뽑아 `cam.aimSlot` 과 공유(복붙하면 한쪽만 고쳐진다). **함정: `RpcServerSubsystem` 의 `SetCatalog` 가 Car·Random 모듈에만 걸려 있어** 새 모듈에서 `prefabName→prefabId` 가 전부 실패했다 — 카탈로그를 쓰는 모듈을 추가하면 그 세 줄을 같이 손볼 것. `scenario.save` 는 월드에 role 정보가 없어 actors 를 못 채운다(한계로 남김) |
| 2026-08-13 | 시나리오 3단계 — `scenario.shoot`/`run` + 가림률 런타임 측정. 실측 시나리오1 36.9%, 시나리오2 99.8% | Rpc/Modules/ScenarioRpcModule.*, Save/3D/Scenario/*.json | **설계서의 "판정은 RPC 밖" 결정을 뒤집었다** — 색 마스크 계수가 20줄이라 런타임에 넣는 편이 낫다(외부 스크립트 없이 `scenario.run` 한 번으로 수치까지). 도색 차체는 명암 폭이 커서 RGB 근접거리로는 못 잡는다 → **채널 비율**(`OnMin>=60 && OffMax < OnMin*0.55`)로 판정하면 magenta/cyan/green 에 그대로 일반화된다. `shoot` 은 `load` 가 선행돼야 한다(role→carNameId 표가 로드 시점에만 생김). `hideRoles` 로 숨긴 차량은 **캡처 실패 시에도** 복원한 뒤 에러를 낸다. 대상이 빈 면인 시나리오5는 칠할 것이 없어 조용히 0%가 아니라 `measured:false` 로 사실을 알린다. 임계값은 조명 의존이라 시나리오 정의에 `lightFile` 이 들어 있다 |
| 2026-08-13 | 시나리오 4단계 — `sweep`(슬롯 순회 측정) + 기획 예상 반증 | Rpc/Modules/ScenarioRpcModule.*, Save/3D/Scenario/occ4_row_stack.json·occ4b_row_oblique.json | **기획서의 "슬롯이 뒤로 갈수록 가림률이 오르는 구배"는 두 카메라 구성 모두에서 틀렸다.** 열 축 위에서는 슬롯 2부터 99% 포화(구배가 아니라 계단), 사선에서는 0~98% 로 퍼지되 오르내린다. 실측이 보여준 지배 변수는 순번이 아니라 **앞차 대비 자기 차의 높이**다(스타렉스 27%·봉고탑차 3.8% vs 모닝 96%·레이 98%) → 난이도를 통제하려면 슬롯이 아니라 차종 조합을 통제해야 한다. sweep 의 "더 가까운 차" 판정은 `slot<k` 가 아니라 **카메라까지 실거리**로 했다(사선·반대쪽 끝에서도 성립). 슬롯→차량은 로드 시 만든 `(presetIdx,slot)→carNameId` 표로 집는다. 가림률이 미세 음수(−4e-4)로 나올 수 있는데 렌더 잡음이며 **클램프하지 않고 그대로 돌려준다**(잡음 규모를 호출자가 알아야 한다) |
| 2026-08-13 | 시나리오 5단계 — `track`(시계열)·`slotPolygon`(빈 면) 측정 + `sim.start` 차종 지정. 가림 5종 전부 수치화 | Rpc/Modules/ScenarioRpcModule.*, Sim/ParkingSimManager.*, SimRpcModule.cpp, RpcServerSubsystem.cpp, Save/3D/Scenario/occ3_passing_car.json | **설계의 "실주행 저속 리플레이 중 연사"를 버렸다** — 캡처 1프레임이 틱 48ms 를 먹어 캡처가 주행을 느리게 만들고 재현성이 깨진다. 대신 경로를 파일에 선언하고 차량을 순간이동시키며 표본을 뜬다(타이밍 의존 0, 잃는 것은 가감속·회피). 결과는 7.5% 기저에 피크 56.9% 의 종 모양 — **기저가 0 이 아니므로 절대값이 아니라 변화량으로 읽어야 한다**(보정하면 기준선이 감춰져 그대로 둠). `slotPolygon` 은 슬롯 코너 4점을 투영해 폴리곤 안 픽셀의 A/B 차분을 센다 — 이 과정에서 **캡처 RT 투영 모델이 검증됐다**(2.5m 폭이 사선 pan 50.3° 에서 1.61m 로 단축돼 `2.5·cos50°` 와 일치). `StartSim` 의 새 인자는 기존 호출부를 안 건드리려고 `OutError` **뒤에** 기본값으로 붙였다. **함정 재발: `SetCatalog` 주입 목록에 SimModule 도 빠져 있었다** — 카탈로그 쓰는 모듈 추가 시 `RpcServerSubsystem` 주입부를 반드시 같이 볼 것 |
| 2026-08-16 | Content 미사용분 분리(5,322개·5.8GB → `Park3D/_UnusedContent`) + 패키지 빌드 복구 | Park3D/Content(이동), .gitignore, Park3D/Config/DefaultGame.ini, BuildPackage.bat | 의존성 그래프(부팅맵·C++·`car_catalog.json` 루트에서 BFS)로 판정 — **UE 는 비-ASCII FName 을 UTF-16LE 로 저장하므로 참조 문자열을 ASCII 로만 훑으면 한글 에셋(차량 23종)이 전부 미사용으로 오판된다.** 6.5GB 중 실제 사용은 75개·796MB. 삭제가 아니라 이동이며 `_UnusedContent/restore_unused.py` 로 되돌린다. 패키지 문제 2건 동반 수정 — ① **`DT_CarCatalog` 를 JSON 으로 대체한 뒤 차량 메시를 가리키는 에셋 참조가 사라져 쿠커가 메시를 통째로 빼먹었다**(에디터는 디스크에서 직접 읽어 멀쩡해 보인다 → 패키지 실행 전엔 안 드러남). `+DirectoriesToAlwaysCook=(Path="/Game/Actors/Car/Meshs")` 로 해결, `.ucas` 254MB→1,780MB ② `Save/`(JSON 참조 데이터)가 스테이징되지 않아 카탈로그·프리셋·카메라를 못 읽었다 → `BuildPackage.bat` 에 robocopy 단계 추가 |
| 2026-08-16 | 패키지 쿡이 83/579 에서 정체 — 에셋 비동기 컴파일 동시성 4로 제한 | Park3D/Config/DefaultEngine.ini `[ConsoleVariables]` | 행이 아니라 **차량 메시 34개 동시 빌드**(개당 요구 1.0~4.5GB)로 커밋 차지가 68.4/69.5GB(98%)까지 차 OOM 직전이었다. 쿡 스레드가 `Waiting for static meshes to be ready 0/34` 로 배치를 기다리는 동안은 `Cooked packages` 카운터가 안 올라가 진행률이 굳어 보인다 — 판정은 로그 갱신 시각·CPU 델타·커밋 차지로 한다. cvar 이름은 `FString::Printf(TEXT("Editor.Async%sCompilation"), AssetType)` 로 **동적 등록**돼 리터럴 grep 으로 안 잡힌다(`AsyncCompilationHelpers.cpp:366`). 수정 후 피크 43.3GB·579/579 완료(쿡 58분). **메시 빌드는 DDC 가 채워지면 사라지는 일회성 벽**이다(중단분 캐시가 남아 재실행 때 34→15개만 재빌드) |
| 2026-08-16 | 하늘·지면 사이 검은 띠 제거 — `AMapFloorActor` 에 20km 원경 지면(`BackdropMesh`) 추가 | Map/MapFloorActor.* | 바닥은 유한(160×160m)한데 `SkyAtmosphere` 는 지평선 **위**만 그려 그 사이가 비어 검게 남았다. 조명이 아니라 지면 지오메트리 문제. 바닥과 같은 머티리얼의 20km 평면을 2cm 아래 깔아 메웠고, 지평선 근처는 대기 산란이 알아서 하늘로 넘겨 준다. **`FloorMesh` 가 루트였어서 그대로 자식을 붙이면 `SetFloorSize` 스케일이 원경까지 곱해진다** → 빈 씬 컴포넌트를 루트로 두고 형제로 붙였다(`AParkingPresetManager` 선례). 20km 평면을 거리필드·Lumen 에 넣으면 씬 간접광과 `scenario.*` 가림률 기준선이 움직이므로 GI 기여에서 뺐다(같은 이유로 `ExponentialHeightFog` 는 채택하지 않음). 원경은 바닥(`MI_Asphalt`)을 부모로 `Tint` 만 0.8 로 낮춘 `MI_Ground` 를 쓴다 — 완전히 같은 머티리얼로 두면 바닥 경계가 사라져 맵 크기 변경의 시각적 피드백이 없어진다. **PIE 중에는 에셋 생성·저장·삭제가 전부 막힌다**(`LogUtils: The Editor is currently in a play mode.` — 디스크에 있는 에셋조차 `save_assets` 가 "does not exist" 로 거절). 에셋 작업은 PIE 를 켜기 전에 끝낼 것 |
| 2026-08-16 | 콘텐츠 교체 후 전체 검정 화면 복구 — 조명 4종 코드 스폰 + 차량 카탈로그 JSON 폴백 + 에셋 경로 재연결 + 신규 머티리얼 4종 | Light/LightControlManager.*, Config/CarCatalogConfig.*, CarPlacementManager.cpp, CarActor.cpp, Map/MapFloorActor.cpp, ParkingPresetManager.cpp, Save/Config/car_catalog.json, Content/M/* | 검정의 원인은 그래픽 설정이 아니라 **부트 맵 `PresetMaker1`(World Partition)의 `__ExternalActors__` 가 콘텐츠 교체 때 통째로 빠져 액터가 0개**가 된 것 — 태양·하늘·노출볼륨이 전부 없었다. 맵이 아니라 코드가 조명을 보장하게 해야 재발하지 않는다(있으면 재사용, 없을 때만 스폰). `ASkyLight` 는 `AInfo` 파생이라 `ALight::SetMobility` 가 없어 컴포넌트에 건다. `DT_CarCatalog` 는 `Content/` 가 gitignore 라 다시 만들어도 또 사라지므로 git 추적되는 `car_catalog.json` 에 `meshDir` 을 넣어 폴백했고, 관문이 `CatalogFromTable` 하나라 여기만 고치면 UI·RPC·시뮬이 같은 카탈로그를 본다. **함정 둘: ① `StopPIE` 는 `HttpServerModule` 의 리스너를 전부 내려 MCP(8000)·RPC(13510)가 같이 죽는다 ② `ConstructorHelpers::FObjectFinder` 는 CDO 생성자에서 한 번만 조회해 static 캐시하므로 에디터 기동 후 만든 에셋은 재기동 전까지 절대 안 물린다** |
| 2026-08-17 | PTZ step 을 Pan/Tilt·Zoom 둘로 분리 + 카메라 뷰어 중앙 십자 | Source/Park3D/CameraControlWidget.*, CameraViewerWidget.* | Pan/Tilt(도)와 Zoom(배율 1~36)은 단위가 달라 step 하나로는 한쪽이 항상 어긋난다(Zoom 기본 0.5). 십자는 `NativePaint` 에서 그린다 — **`Img_View` 를 Overlay 로 감싸면 안 된다**(`ApplyViewSize`·드래그가 `Cast<UCanvasPanelSlot>(Img_View->Slot)` 에 의존해 크기조절·이동이 통째로 깨진다). 함정 둘을 또 밟았다 — ① 색·두께를 `UPROPERTY` 로 노출(같은 파일에 이미 'UPROPERTY 추가하면 패키지가 Bad export index 로 즉사' 경고가 달려 있었다) → `static constexpr` ② 안 보이는 원인을 좌표로 의심했으나 **대비 문제**였다(흰색 75% 가 밝은 노면에 묻힘). **'안 보인다'는 위치보다 대비를 먼저 의심하고, 진단용 원색을 한 번 칠해 보면 즉시 갈린다.** 해법은 어두운 테두리 + 밝은 심의 이중선 |
| 2026-08-18 | 패널 공통 정리 — 구분선·높이 자동맞춤을 `Park3DPanelStyle` 로 올려 네 패널이 공유 | Source/Park3D/Park3DPanelStyle.h, CameraControlWidget.*, PresetMakerWidget.*, CarPlacementWidget.*, RenderPanelWidget.* | **"다른 컴퓨터가 이전 UI를 푸시했다"는 신고는 사실이 아니었다** — `origin/main` 에 우리 UI 코드가 전부 살아 있었고(다른 컴퓨터의 11커밋은 번호판·Env RPC·저장 데이터), 실행 중이던 `Package/Windows` 의 exe 가 낡은 중간 빌드였다. **"옛 UI가 보인다"는 소스→빌드→배포 exe 세 단계 중 어디가 낡았는지부터 가른다.** Content(WBP)는 git 제외라 git 으로 UI 자산이 오갈 수 없다는 점도 근거. 공용화하면서 얻은 것 — 패널마다 본문 열 이름이 달라(BindWidget 으로 노출된 곳은 카메라뿐) **이름 대신 구조로 찾는다**(`FindContentColumn`: 자식이 가장 많은 VerticalBox). 기준 위젯이 열의 0번이면 그 앞에는 선을 못 넣으므로 패널별 삽입 개수가 다르다(카메라 4·차량 3·프리셋 1) — 삽입 결과는 로그로 확인할 것. 패널 열기 자동화 메모: 좌표 클릭은 창을 앞으로 올린 뒤에만 먹고 첫 클릭은 활성화에 먹힌다, `PostMessage` 마우스 메시지는 Slate 가 무시한다, 캡처는 `PrintWindow(PW_RENDERFULLCONTENT)` 로 가려져도 된다 |
| 2026-08-18 | 카메라 패널 버튼 재배치(PTZ 패드 옆 칸) + 그룹 구분선 + 독 아이콘 80% | Source/Park3D/CameraControlWidget.*, MainMenuWidget.*, Tools/wbp/{ui_kit,panel_menu}.py | **"수정이 안 먹었다"의 정체는 카드 밖으로 나간 것이었다** — 로그를 심어 보니 재배치는 실행 중이었고, 카드 폭이 캔버스 슬롯에 고정인데 `FitPanelToContent` 가 높이만 맞춰 옮긴 버튼이 어두운 카드 **바깥**(3D 화면 위)에 글자만 떠 있었다. 값이 들어갔는데 화면이 다른 사례가 이 프로젝트에서 세 번째다(`light.get`·아이콘 브러시) → UI 가 안 먹은 것처럼 보이면 코드 실행 여부부터 로그로 가른다. 폭을 늘리는 해법은 "패널 폭은 원래대로" 지시로 되돌리고 **패드를 줄여**(`GPtzPadScale` 1.5→1.0) 원래 폭 안에 옆 칸을 냈다 — 네 버튼이 한 줄인 저장/열기/초기화/피킹은 좁은 칸에서 라벨이 잘려("카메라 피킹 시…") 패드 아래 전체 폭에 남긴다. **여백을 상수로 찍으면 안 된다** — `GPanelChromeHeight=16` 이 제목줄을 빼먹어 하단 줄이 잘렸다 → `카드 높이 − 스크롤 영역 높이` 로 실측하고 매 틱 재보정(1회 실행 잠금도 해제 — 첫 성공 시점 높이는 줄 이동·구분선 반영 전 값이다). 구분선은 검정 28%로 그으면 어두운 카드에 묻힌다 → 흰색 35%. 검증은 패키지 복제(`Package/Work13530`)에 exe 만 갈아끼워 했다 — 개발 exe 단독 실행은 에디터 Zen 스토리지가 죽으면 같이 죽는다. 창 캡처는 `PrintWindow(PW_RENDERFULLCONTENT)` 로 가려져도 되지만 **클릭은 실제 입력이어야 한다**(`PostMessage` 는 Slate 가 무시). 좌표 클릭은 창 크기·최상단 여부를 검사하지 않으면 남의 창이나 `종료` 를 누른다(실제로 눌러 인스턴스가 꺼졌다) |
| 2026-08-18 | 그림자가 너무 어두운 문제 — 채움광 2종(그늘 채움·차량 채움) 추가. 패널 2줄 + `light.*` 필드 + JSON 키 | Source/Park3D/Light/*, Rpc/Modules/LightRpcModule.cpp, Save/3D/Light/LightSettings.json | **UE5.8 에는 `ShadowAmount`(그림자 농도)가 없다** — 그림자만 연하게 하는 knob 이 없어 그늘에 빛을 더하는 수밖에 없다. UDS 레벨에서는 태양·하늘빛·노출이 전부 무력화되므로(2026-08-17), **우리가 스폰한 그림자 없는 DirectionalLight 2개**를 더한다(UDS 는 자기 액터만 갱신해 이걸 못 덮는다). 차량용은 **라이팅 채널 1** 전용이고 차량 메시는 채널 **0+1** 로 둔다 — 0 을 빼면 차량이 태양·하늘빛을 못 받아 떠 보인다. 방향을 태양에 따라가게 만들면 UDS 가 태양을 돌릴 때 "고정 밝기" 가 깨지므로 고정(피치 −60°). 실측 기본값 그늘 2.0 / 차량 6.0 — 그늘 2 에서 그늘 노면 +49 인데 양지 노면은 +3(채움광 특성), 차량 6 에서 그늘 차량 131.1 ≈ 양지 차량 131.3 으로 "고정" 달성(10·15 는 그늘이 양지보다 밝아져 역전). **UDS Time of Day 가 흘러 A/B 는 한 프로세스에서 연속 촬영해야 한다**(60초 사이 그늘 53→103 이동). 구조체 기본값은 0 으로 두고 실제 값은 `LightSettings.json` 으로 준다(기존 조명 파일 호환 + `scenario.*` 기준선 보존). **함정: 복제 인스턴스 config 에 `level` 을 빠뜨리면 빈 부트맵으로 떠 매니저가 태양을 스스로 스폰하고 화면이 통째로 깨진다 — 조명 증상을 재현할 수 없다** |
| 2026-08-18 | 파일의 카메라 위치가 패널 min/max 밖이면 조용히 잘리던 문제 수정(경계를 값까지 넓힘) | Source/Park3D/CameraControlWidget.cpp | `FCamDir` 에는 **Pan/Tilt/Zoom 범위(`ptzmin`/`ptzmax`)만** 있고 위치 3종은 없다 → 화면에 남아 있던 범위로 클램프돼 파일값이 알림 없이 잘렸다(넓은 주차장·범위를 좁혀 둔 뒤 다른 파일 열기). 파일 형식을 늘리는 대신 로드 시 경계를 값까지 넓힌다 — 클램프를 없애면 `Cur ∈ [Min,Max]` 가 깨져 슬라이더와 입력칸이 어긋나고 다음 조작에서 값이 튄다. min/max 를 뒤집어 적어 둔 경우까지 감안해 실제 아래/위 칸을 골라 넓힌다. 실측: 파일 `(75,-63,22)` → 수정 전 `(50,0,5)`, 수정 후 `(75,-63,22)` |
| 2026-08-18 | 파일열기 시 카메라 위치(높이·X·Z)가 첫 프리셋 값으로 안 들어가던 문제 수정 | Source/Park3D/CameraControlWidget.* | **UE5.8 `USlider::SetValue` 는 값이 달라지면 `OnValueChanged` 를 브로드캐스트한다**(Slider.cpp:41) — 코드 주석의 "프로그램적 SetValue 는 재진입 없음"은 틀렸다(`UEditableTextBox::SetText` 는 실제로 안 쏜다. 둘을 같이 믿으면 안 된다). `FillControlsFromDir` 이 컨트롤을 순서대로 채우는 도중 첫 `SetVal(X)` 이 슬라이더 콜백으로 되돌아와 `SyncCameraPosAcrossPresets` 를 부르는데, 그때 컨트롤은 X 만 새 값이고 높이·Z 는 이전 값이었다. 그 혼합값이 `CamData` 프리셋에 덮이고 **`Dir` 이 바로 그 배열 원소의 참조**여서 이어지는 `SetVal(높이/Z)` 가 파일값 대신 방금 덮인 값을 읽었다. 선택 카메라만 증상이 난 이유 — 나머지는 `SyncCamerasToData` 경로라 무사, Pan/Tilt/Zoom 은 프리셋을 안 건드려 무사. 수정은 채우는 동안 슬라이더 콜백을 막는 플래그(비-UPROPERTY) + `Dir` 값 복사. 검증은 패키지 복제에 exe 만 교체해 기동 직후 `cam.list` A/B — 수정 전 `(-7.029, 0, 5)` / 수정 후 `(-7.029, -1.227, 4.5)` |
| 2026-08-17 | 패널 원본 복원 + 다크 스타일 + 카메라 위치를 카메라 단위 속성으로 | Source/Park3D/Park3DPanelStyle.h(신규), CameraControlWidget.*, PresetMakerWidget.*, MainMenuWidget.*, Tools/wbp/* | 재구성한 UI 가 "가독성이 떨어진다" 는 판단을 받아 **원본 WBP 를 되돌렸다** — 처음에 "복구 불가"라고 단정했으나 틀렸고, 이전 작업본 `D:\Work\UnrealWork\Parking\Park3D\Content\UI` 에 그대로 있었다(**이 프로젝트는 작업본이 여러 폴더에 있다 — 확인 범위를 다 훑기 전에 복구 불가를 말하지 말 것**). 복사 후 컴파일이 통과해 현재 C++ 과 정확히 맞음을 확인. 원본 위에 스타일만 입힌다(배치 무수정). **스크립트로 못 넣는 스타일이 있다 — 아이콘·슬라이더·체크박스는 `set_properties` 로 값이 들어가도 화면에 안 나온다** → `Park3DPanelStyle::ApplyToTree` 로 C++ 에서 처리(엔진 기본 스타일에서 출발해 색·크기만 바꿔야 Hovered/Disabled 를 안 놓친다). `GetWidgets` 의 `bInherited=true` 를 거르면 BindWidget 위젯(슬라이더·체크박스·RootBorder)을 통째로 놓친다 — 경로 유무로만 거를 것. `RootBorder` 는 BindWidgetOptional 로 예약돼 `RenameWidget` 이 거부한다. 카메라 높이·X·Z 는 프리셋이 아니라 **카메라의 속성**이라 `SyncCameraPosAcrossPresets` 로 위치 변경·카메라 전환·파일 열기 세 지점에서 동기화 |
| 2026-08-17 | UI 를 이전 BP 디자인으로 재구성 — 하단 아이콘 독 + 검은 카드 + 슬라이더 3열. WBP 5종 재구성, VLA 버튼·뷰어 이미지·차량 랜덤 독 버튼 제거 | Source/Park3D/MainMenuWidget.*, Tools/wbp/* | **`.uasset` 에서 위젯 이름만 뽑아서는 디자인을 알 수 없다** — 사용자가 준 실제 화면 3장이 규약을 알려줬다(슬라이더 아래 최소·현재·최대 3열, 흰 입력칸에 검은 글자, 패널만 검정). 카메라 패널이 24줄→6줄로 줄었다. **아이콘을 WBP 브러시에 넣으면 안 된다** — `ResourceObject`·`ImageType`·`DrawAs` 를 다 맞춰 브러시 값이 완벽해져도 화면은 빈칸이었고, C++ `LoadObject`+`SetBrushFromTexture` 만 됐다. **브러시 값이 맞다고 그려지는 게 아니다**(조명 때 `light.get` 에 속은 것과 같은 함정). 슬롯은 `AddWidget` 응답에서 챙겨야 한다 — `GetWidgets` 로 되찾으면 `"None"` 이 와서 패딩·앵커가 조용히 안 먹는다(독이 상단에 붙는 증상). `GetWidgets` 는 BindWidget 이름을 `bInherited=true` 로 함께 주므로 트리 조작에서 걸러야 한다. `Border` 배경은 `DrawAs` 를 줘야 그려진다. 이모지는 UE 기본 폰트에 글리프가 없어 빈 사각형이 된다 → `Widgets/Icons/TabIcons` 원본 사용. **WBP 자산은 지우지 말고 트리만 비운다**(지우면 `TSubclassOf` 참조가 끊긴다) |
| 2026-08-17 | 차량 랜덤 패널(`WBP_RenderPanel`) 신설 — 이전 BP UI 의 렌더 패널 복원. WBP 를 스크립트로 제작 | Source/Park3D/RenderPanelWidget.*, MainMenuWidget.*, Park3D.uproject, Tools/wbp/* | **WBP 를 코드로 만들 수 있다 — 2026-08-05 의 "제작 불가" 판단은 이제 낡았다.** 엔진에 `UMGToolSet`(Experimental/Toolsets, "UMG widgets via reflection") 이 있고 이것만 개별로 켜면 된다. **`AllToolsets` 로 켜지 말 것** — `GameFeatures` 가 딸려 와 쿡이 깨진다(`2fa59d0` 이 고쳤던 그 문제). 스킬에 적힌 `add_widget` 계열은 그 커밋으로 등록이 끊겨 지금은 없다. MCP 는 툴셋 구조라 **실제 툴은 `call_tool` 을 거쳐 부른다**(`tools/list` 에는 3개만 보인다). 세션 시작 시 에디터가 꺼져 있으면 MCP 툴이 안 붙으므로 `Tools/wbp/umcp.py` 로 HTTP 를 직접 말한다(`Mcp-Session-Id` 헤더 + `notifications/initialized` 필요). 자산 참조는 **오브젝트 전체 경로**(`/Game/UI/WBP_X.WBP_X`), 슬롯 값은 `LayoutData` 로 감싼다. **루트를 Border 로 두면 화면 전체를 덮는다** → 루트는 `CanvasPanel`. 컴파일 성공이 곧 BindWidget 검증이다. **WBP 는 `Content/` 라 커밋에 없다** → `Tools/wbp/` 스크립트로 재생성 |
| 2026-08-17 | 환경 콘텐츠 5,322개 복원 + 부팅맵을 `LV_Park_01` 로 교체 + `map_floor` 설정 신설 + 조명 탐지를 컴포넌트 기준으로 + 시작 조명 0.5초 지연 | Config/DefaultEngine.ini, Save/Config/config_pmaker.json, Save/3D/Light/*, Config/Park3DAppConfig.*, Light/LightControlManager.*, Park3DGameMode.* | "렌더링을 참조 이미지처럼" 요청의 실체는 셰이더가 아니라 **씬에 환경이 없는 것**이었다 — 2026-08-16 분리의 BFS 루트가 이미 액터가 빈 `PresetMaker1` 이라 환경 전체가 미사용으로 판정됐다. 레벨 판별용 `SceneCapture2D` 자동 스크린샷은 5회 모두 실패했다(스카이돔이 바운즈 지배 / RT float→EXR / spawn 의 rotation 무시 / **SceneCapture 는 텍스처 스트리밍에 뷰를 등록하지 않아 밉이 안 뜬다**) → 렌더 대신 배치된 메시 이름으로 판별했다. `FindSun` 이 `ADirectionalLight` **액터만** 봐서 UDS 가 품은 태양을 놓치고 두 번째 태양을 스폰했다(이중 조명) → 컴포넌트까지 훑는다. **조명은 결국 UDS 에 전부 맡겼다** — 세 번 뒤집힌 끝의 결론이다. ① 컴포넌트 배열에서 `Moon` 이 `Sun` 보다 앞서 달을 집었고 ② `IsUsedAsAtmosphereSunLight()` 로 태양을 확정해도 하늘·구름·하늘빛은 라이트가 아니라 `Time of Day`(30분 주기로 흐른다)가 지배했고 ③ 시간을 낮으로 고정해도 우리 unbound `PostProcessVolume` 이 UDS 자체 포스트프로세스를 덮었다. `HasExternalSkySystem()`(태양이 `ADirectionalLight` 액터가 아니면 참)으로 적용·생성을 통째로 건너뛰니 비로소 한낮이 됐다. **`light.get` 이 방금 넣은 값을 돌려주는 것은 적용의 증거가 아니다 — 값 유지와 화면은 별개이고 이 프로젝트에서 두 번 속았다. 조명 검증은 반드시 캡처 이미지로 한다.** 대가: 이 레벨에서 조명 패널·`light.*`·`scenario.*` 의 조명 통제가 무력해진다(밝기는 UDS `Time of Day` 로). `light.setDefault` 는 `_default.txt` 에 절대 경로를 써서 패키지(`Save/` 가 ProjectDir 밖)를 깨뜨린다 → 파일명만 유지. 패키지 쿡은 레거시 BP UI 세트(`/Game/Core`·`/Game/Widgets`)가 없는 파일 다이얼로그 플러그인을 불러 35건 컴파일 에러를 내지만 실행에 안 쓰이므로 `-IgnoreCookErrors` 로 통과시킨다. 주차면·차량 좌표는 우연히 그대로 맞았다 |
| 2026-08-14 | RPC 표면 확장(108→116) — `car.catalog`(메시 실측 치수)·`car.update`·`cam.deletePreset`·`preset.setView`·`scenario.batch/delete`·`shot.list/clear` | Rpc/Modules/{Car,Cam,Preset,Scenario}RpcModule.cpp | 지시: UI 패널을 조작하지 말고 **패널이 하는 기능**을 RPC 로 올릴 것. **차량 메시 축 매핑은 X=전폭·Y=전장·Z=전고다** — 처음에 +X 를 길이로 가정했다가 BMW 전장이 2.08m 로 나와 드러났다. 전폭이 실차보다 0.2~0.4m 큰 건 사이드미러가 바운즈에 포함돼서이며 실루엣 기준으로는 그게 맞다. 이 실측으로 **시나리오4 의 "차종 높이가 지배 변수" 해석이 관측 상관에서 측정 근거로 확정**됐다(높이차 +0.88→가림 4.0%, −0.67→97.8%, 단조 반비례). `car.catalog` 는 메시를 동기 로드하므로 최초 1회 수 초 걸리고, 못 읽으면 0 이 아니라 `meshLoaded:false` 로 알린다. **VLA 메뉴 2종은 `BlueprintImplementableEvent` 라 C++ 에서 기능을 알 수 없어 RPC화 불가** — 블루프린트 내용 확인이 선행돼야 한다 |
| 2026-08-18 | 나무 영구 제거 요청 — 저장소 변경 없음. 배포본의 낡은 `Save/config` 만 갱신 | Docs/20260818_104643_*.md(진단 기록), Package/Windows/Save(빌드 산출물) | 코드도 설정도 이미 있었다(`hide_actors: [StaticMeshActor_188]`, 커밋 `409a04d`)는데 화면엔 나무가 그대로였다. **exe 는 최신(08-18 09:55)인데 배포본의 `Save/` 는 전날 18:23 자였다** — 이 프로젝트의 상용 절차인 "패키지 복제 후 exe 만 갈아끼우기"가 정확히 이 상태를 만든다(코드만 갱신, `Save/` JSON 참조 데이터는 잔류). **"설정을 넣었는데 안 먹는다"는 저장소 config 가 아니라 실행본이 읽는 config 의 `LastWriteTime` 을 먼저 본다.** 판정 근거는 로그 `[Env] config hide_actors 적용: n/m` — 줄이 아예 없으면 낡은 config, `0/1 못 찾음`이면 이름 불일치라 둘이 갈린다. 화면 속 물체 → 액터 이름 역산은 `view.pick`(픽셀 기준면은 창이 아니라 **렌더타깃 960×540**, 반환은 m) → `env.list`(`near`/`radius` 는 **cm**) 순인데, pick 은 나뭇잎을 통과해 지면을 맞히므로 확정은 바운즈 크기로 한다 |
