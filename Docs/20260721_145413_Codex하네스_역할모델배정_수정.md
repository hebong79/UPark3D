# Codex 하네스 역할별 모델 배정 수정

- 작성일시: 2026-07-21 14:54:13
- 작성자: Codex
- 참고: `AGENTS.md`, `.codex/agents/*.toml`, `.agents/skills/parking-dev-orchestrator/SKILL.md`, `.agents/skills/parking-cpp-loop/SKILL.md`
- 선행: [Codex 하네스 역할별 모델 설정](20260720_161746_Codex_하네스_역할별모델_설정.md)

---

## 1. 변경 파일

| 파일 | 변경 내용 |
|---|---|
| `AGENTS.md` | 검증·테스트(`qa-verifier`) 담당 모델을 `gpt-5.6-terra`로 변경 |
| `.codex/agents/qa-verifier.toml` | QA 에이전트 기본 모델을 `gpt-5.6-terra`로 변경 |
| `.agents/skills/parking-dev-orchestrator/SKILL.md` | 역할별 모델표에서 QA와 문서화 모델을 분리해 명시 |

## 2. 확정 모델 배정

| 작업 | 역할 | 담당 모델 |
|---|---|---|
| 설계·영향도 분석 | architect, impact-analyst | `gpt-5.6-sol` |
| 개발 | unreal-implementer | `gpt-5.6-terra` |
| 검증·테스트 | qa-verifier | `gpt-5.6-terra` |
| 한글 문서화 | doc-writer | `gpt-5.6-luna` |

Goal/Loop 작업은 일반 역할 배정을 적용하지 않는다. 설계, 영향도, 구현, 사전점검, 실행, Automation/PIE 검증, 재설계 반복을 모두 `gpt-5.6-terra`가 단독 수행하고, 완료 뒤 최종 `Docs/yyyyMMdd_HHmmss_이름.md` 작성만 `gpt-5.6-luna` doc-writer가 맡는다.

## 3. 검증 결과

- `AGENTS.md`의 역할 표, `qa-verifier.toml`의 기본 모델, 오케스트레이터 스킬의 모델 표가 모두 같은 배정으로 일치하는지 정적 검색으로 확인한다.
- Goal/Loop 전용 스킬과 `AGENTS.md`는 이미 Terra 단독 수행 및 Luna 문서화 규칙을 갖고 있어 변경하지 않았다.
