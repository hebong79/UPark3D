# 차량배치 4단계(1) 매니저 구현·검증 보고서

- **일시**: 2026-06-24 11:34
- **단계**: 설계서 §10.1 4단계 중 **매니저**(`ACarPlacementManager`). 위젯(`UCarPlacementWidget`)은 후속.
- **결과**: ✅ 구현·빌드(Live Coding)·유닛테스트(10종) 통과.

---

## 1. 신규 파일 (3개, 기존 코드 수정 0)

| 파일 | 내용 |
|------|------|
| `CarPlacementManager.h/.cpp` | 차량 데이터→월드 액터 생성/제거/조회/선택/역변환 + 픽. `AParkingPresetManager` 스타일 |
| `Tests/CarPlacementManagerTest.cpp` | 카탈로그 해석 + RebuildAll/선택/역변환/정리 검증 |

## 2. 인터페이스 (§4.2)

```
void RebuildAll(const FCarPosDatas&, const TArray<FCarPresetEntry>& Catalog, const TArray<int32>& Sel);
bool RebuildFromFile(const FString& JsonPath, UDataTable* CatalogTable, const TArray<int32>& Sel);  // 위젯 "열기"
ACarActor* SpawnCarFromPos(const FCarPos&, const TArray<FCarPresetEntry>& Catalog);
void ClearAll();  int32 GetCarCount();  ACarActor* GetCar(int32);  ACarActor* FindByNameId(const FString&);
void SetSelectedIndices(const TArray<int32>&);   FCarPosDatas ToCarPosDatas() const;   // 저장용 역변환
static TArray<FCarPresetEntry> CatalogFromTable(UDataTable*);     // DT_CarCatalog → 배열
bool TraceFloor(APlayerController*, FVector& Out);  ACarActor* TraceCar(APlayerController*);  // 픽
static const FCarPresetEntry* FindEntryByPrefabId(const TArray<FCarPresetEntry>&, int32);
```
- 메시 해석: `prefabId == FCarPresetEntry.Idx` 매칭, 없으면 첫 항목 폴백.
- 차량 액터: `CarActorClass`(기본 `ACarActor`)로 스폰, `CarTag` 부여, `InitFromPos`로 트랜스폼 적용.

## 3. 유닛테스트 (10종, Park3D.CarPlacement.* — 전종 통과)

신규 2종:
- `CatalogLookup` — `FindEntryByPrefabId`: Idx 매칭/폴백/빈 카탈로그 nullptr.
- `ManagerRebuild` — 에디터 월드에 매니저 스폰 → 3대 RebuildAll(메시 없이 트랜스폼/선택 로직) → 개수·위치(UnityPosToUE 일치)·선택(인덱스 1만)·이름검색·역변환 라운드트립·재선택·ClearAll 검증 후 파괴.

기존 8종(1~3단계)도 함께 통과.

## 4. 빌드 이슈(수정)

- C2445: 삼항 `CarActorClass ? CarActorClass : ACarActor::StaticClass()` 의 `TSubclassOf` ↔ `UClass*` 불일치 → `CarActorClass.Get()`으로 `UClass*` 통일.

## 5. 영향도

- 신규 파일만 추가, 기존 기능 무영향. 매니저는 별개 액터 + 차량 충돌 QueryOnly라 PresetMaker 트레이스와 충돌 없음.
- Python 슈가 바인딩은 Live Coding 후 미생성이라, 매니저 검증은 **C++ 자동화 테스트로 수행**(python UFUNCTION 호출 불가 회피).

## 6. 다음 (4단계 위젯)

`UCarPlacementWidget`(차량배치 대화상자) — `UPresetMakerWidget` 패턴(BindWidget + 핸들러 + JSON + GetCarManager + 패널 드래그). 이어서 C++ 작성 → 컴파일 → `WBP_CarPlacement`(MCP 제작) → 동작 확인.
