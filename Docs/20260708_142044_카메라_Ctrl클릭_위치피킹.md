# 카메라 위치 Ctrl+좌클릭 피킹 — 구현·검증 보고서

- 작성일: 2026-07-08
- 실행: `parking-cpp-loop`(C++ 전용, 컴파일만 수동 게이트)
- 결과: **반복 1회차 성공** (컴파일 1회 수동, 나머지 MCP 자동)
- 대상: `CameraControlWidget.h/.cpp`

## 1. 요구사항
1. "카메라 피킹 시작" 누르면 피킹모드 On → Ctrl+좌클릭 맵 바닥 시 카메라 위치(+폴대) 이동.
2. 버튼 색: On=붉은색, Off=처음색.
3. 반드시 플래그 변수(bPicking) 설정.
4. 다른 대화상자(프리셋메이커/차량배치) 피킹과 충돌 금지, 독립 동작.

## 2. 기존 인프라(재사용)
- `bPicking` 플래그, `HandlePicking`(`RequestPick(CamPos)`/`ReleasePick` 전역 배타 중재).
- `ACameraControlManager::TraceFloor(PC, OutWorld)` — 폴대 무시 바닥 위치.
- `ApplyControlToCamera(X)` — Cur(X/H/Z, Unity m) → `UnityPosToUE` → `SetCameraWorldLocation`(카메라+폴대 이동).
- `UCameraControlLibrary::UEToUnityPos` — 역변환. `SetControlCurText/SetControlSlider`, `GetControlMin/Max`.
- CarPlacement 피킹 선례(`bOverPanel=RootBorder->IsHovered()`, `bCtrl`, `WasInputKeyJustPressed(LMB)`).

## 3. 변경 내용
### (H) 헤더 — 원색 저장 멤버 1개
```cpp
FLinearColor PickBtnDefaultColor = FLinearColor::White; // 피킹버튼 원색(기본상태 캡처)
```
### (C1) NativeConstruct — 원색 캡처 + 상태색 적용
```cpp
if (Btn_Picking)
{
    if (!bPicking) { PickBtnDefaultColor = Btn_Picking->GetBackgroundColor(); }
    Btn_Picking->SetBackgroundColor(bPicking ? FLinearColor::Red : PickBtnDefaultColor);
}
```
### (C2) HandlePicking — On=붉은색 / Off=원색
- On: `Btn_Picking->SetBackgroundColor(FLinearColor::Red);`
- Off: `Btn_Picking->SetBackgroundColor(PickBtnDefaultColor);`
### (C3) NativeTick — Ctrl+좌클릭 → 카메라(+폴대) 이동
```cpp
APlayerController* PC = GetOwningPlayer(); if (!PC) return;
const bool bCtrl = PC->IsInputKeyDown(EKeys::LeftControl) || PC->IsInputKeyDown(EKeys::RightControl);
const bool bOverPanel = (RootBorder && RootBorder->IsHovered());
if (bPicking && bCtrl && !bOverPanel && PC->WasInputKeyJustPressed(EKeys::LeftMouseButton))
{
    ACameraControlManager* Mgr = GetCameraManager(); FVector HitWorld;
    if (Mgr && Mgr->PickMode == EPickMode::CamPos && Mgr->TraceFloor(PC, HitWorld))
    {
        const FCamVec3 U = UCameraControlLibrary::UEToUnityPos(HitWorld, MetersToUU);
        // X/Z만 갱신(높이 유지, 슬라이더 범위 클램프) → ApplyControlToCamera(X) 일괄 반영
        ... SetControlCurText/SetControlSlider(X,Z) ...
        ApplyControlToCamera(ECamCtrl::X);
        Notify(TEXT("카메라 위치 이동: X=.. Z=.. (m)"));
    }
}
```
- include: `GameFramework/PlayerController.h`.

## 4. 충돌 방지·독립성 (요구 4)
- **배타 패널 토글**(선행 작업): 한 번에 1개 패널만 표시. 숨겨진 패널은 NativeTick 미실행 → 카메라 피킹은 카메라 패널이 열린 동안에만 동작.
- 3중 가드: `bPicking`(위젯 전용 플래그) + `bOverPanel`(카메라 패널 호버 제외) + `Mgr->PickMode==CamPos`(매니저 전역 배타 중재).
- 프리셋/차량은 각자 플래그·NativeTick으로 독립. 좌표는 X/Z 슬라이더 범위로 클램프.

## 5. 검증 (실PIE)
| 항목 | 방식 | 결과 |
|------|------|------|
| 컴파일 | 사용자 Ctrl+Alt+F11(PIE 정지 상태, 헤더 변경) | ✅ succeeded(05:15:18) |
| 버튼 색상 On=붉은색 | 사용자 시각 확인 | ✅ 변경됨 |
| Ctrl+좌클릭 카메라(+폴대) 이동 | 로그 `카메라 위치 이동` ×6 (X/Z 정상값) | ✅ 이동 확인 |
| 피킹 On/Off | 로그 `피킹 시작`/`피킹 종료` | ✅ |
| 플래그(bPicking) | 시작/종료 게이트 동작 | ✅ |

로그 발췌:
```
[CameraControl] 카메라 위치 피킹 시작 — Ctrl+좌클릭
[CameraControl] 카메라 위치 이동: X=5.26 Z=-6.83 (m)
[CameraControl] 카메라 위치 이동: X=8.60 Z=-2.47 (m)  ... (총 6회)
[CameraControl] 카메라 위치 피킹 종료
```

## 6. 영향도
- `CameraControlWidget`만 변경(헤더 멤버 1 + cpp 3곳 + include 1). 다른 위젯/매니저/로직 무관.
- `PickBtnDefaultColor`는 비-UPROPERTY 런타임 상태 → 직렬화/GC 영향 없음.
- NativeTick에 매 프레임 입력 체크 추가(경량, 피킹 Off 시 조기 분기).
- 헤더(레이아웃) 변경 → Live Coding 시 PIE 정지 후 컴파일 권장(이번에 적용).

## 7. 산출물
- 코드: `CameraControlWidget.h`(멤버), `.cpp`(NativeConstruct/HandlePicking/NativeTick/include)
- 설계서: `_workspace/cam_pick_ctrlclick_design.md`
- 본 보고서: `Docs/20260708_142044_카메라_Ctrl클릭_위치피킹.md`

## 8. 미결/참고
- 폴대 클릭 선택(TracePole→Combo 동기)은 범위 밖(별도 기능).
- 좌표는 X/Z 슬라이더 범위로 클램프 — 범위 밖 바닥 클릭 시 경계로 이동. 무제한 이동 필요 시 범위 확장 별도 검토.
- (직전) 콤보 드롭다운 흰색 작업은 스크린샷 검증 미완 — 사용자 확인 대기.
