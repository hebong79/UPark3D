# 데칼 표시 창 열기 기본값 사후 영향도

- 작성일시: 2026-07-21 13:12:24

| 영향 면 | 사후 결과 | 위험도 |
|---|---|---:|
| 변경 파일 | `PresetMakerWidget.cpp` 한 곳, `NativeConstruct()`의 `Check_UseDecal` 블록만 변경 | 낮음 |
| 빌드/헤더 | 신규 모듈·include·헤더 변경 없음. UE 5.8 UBT 컴파일 성공 | 낮음 |
| 위젯/Blueprint | BindWidget 이름·타입과 WBP 에셋은 수정하지 않음. 체크박스가 있을 때만 기본 true 적용 | 낮음 |
| 매니저/렌더 | 첫 `RefreshView()`부터 `RebuildDecals` 활성 경로가 기본값이 됨. 데칼 Off 경로는 미변경 | 중간(의도된 표시 기본값 변경) |
| JSON/저장 데이터 | 변경 없음. 체크 상태는 저장되지 않는 뷰 상태 | 낮음 |

## 잔여 확인 사항

- 실제 PIE에서 시작/패널 재열기 시 체크 표시와 데칼 렌더가 보이는지는 현 세션 도구 제약으로 미확인이다.
- 사용자가 체크를 해제한 뒤 기존 디버그 라인 표시로 복귀하는 상호작용도 같은 이유로 미확인이다.
